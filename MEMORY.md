<!-- Agent reads this file at every session start. Surface any entry marked PENDING CONFIRMATION
to the human before proceeding. Do not act on a pending entry — wait for explicit confirmation
or rejection. -->

2026-06-04 · Redesigned the admin Media Library into a dual-pane "Darkroom Gallery" split-view workspace with 1:1 image tiles, a sticky details panel with copyable HTML embed and direct URL inputs, and an interactive drag-and-drop/browse upload zone. Added dynamic CSS cache busters to layout.php.
2026-06-04 · Standardized copyable inputs with a border-collapsed side-by-side design, unified destructive actions with a new `.admin-btn-danger` outline button style, and enforced Safari mobile compatibility with zero rounded corners.
2026-06-04 · Reworked the admin Media Library into an admin-native grid manager with asset cards, shared admin button/input styling, a reusable notice pattern, and a wider content mode instead of the earlier bespoke split-gallery treatment.
2026-06-04 · Fixed admin view layout include paths so `/admin` pages consistently render through the shared admin layout and navigation rather than risking public-site chrome.
2026-06-04 · Migrated all site typography to self-hosted woff2 files (Lora, Pinyon Script, Courier Prime) under public/assets/fonts/ to eliminate all off-domain Google Fonts CDN requests, introducing head preloads and block-rendering displays to completely resolve FOUT (Flash of Unstyled Text).
2026-06-04 · Designed and built a lightweight, hardware-accelerated celestial background containing slow-drifting cyan/magenta/gold watercolor nebula washes and a rotating SVG astrolabe/coordinate grid.
2026-06-04 · Updated cosmos.js to color-code dynamically spawned stars using realistic astrophysical temperature distributions, animated the #cosmos-stars container to rotate clockwise slowly (60s loop) around the screen center, and refactored canvas shooting stars into noticeable 10-second clockwise orbital comets with color-shifting HSL gradient tails that share the stars' center of rotation (with speeds scaled proportionally to radius).
2026-06-05 · The public layout now centralizes SEO/social metadata, canonical URLs, optional `robots`, `og:image:alt` / `twitter:image:alt`, skip-link behavior, `aria-current` nav states, and progressive-enhancement hooks for gallery/work expanders.
2026-06-05 · Public-facing accessibility and responsiveness were tightened across the gallery, work, category, exhibit, managed page, and legacy About views with labelled sections, decorative `aria-hidden` markers, better touch targets, low-power/reduced-motion fallbacks, and contact-form status/error semantics.
2026-06-05 · Current font delivery is mixed: the public site and authenticated admin shell use self-hosted `Lora`, `Pinyon Script`, and `Courier Prime`, but `app/views/admin/login.php` still requests Google Fonts for the login screen.
2026-06-05 · Navigation is now managed through a unified `navigation_items` registry that auto-bootstraps once, non-recursively, and only creates or updates nav records without touching the underlying pages themselves.
2026-06-05 · `/admin/navigation` supports mixed system/page/external ordering with visible and hidden sections, explicit hide/restore controls, external-link hard delete, inline external-link label editing, and a dedicated `New Tab` switch column for external links only.
2026-06-05 · Refactored the public mobile/tablet navigation to a slide-out drawer on viewports < 900px, eliminating the complex dynamic JS item-by-item collision loop. Inline links hide and the hamburger toggle shows via CSS media queries targeting the `.js-enhanced` class, preserving a clean inline fallback when JS is disabled. The drawer is pre-populated once on load, preventing empty menus. The site title is configured to wrap on screens under 480px.
2026-06-05 · Artwork iframe embeds are now considered valid based on markup only: the saved piece must contain a real `<iframe>` with an extractable `src`, and the app no longer auto-rejects embed URLs just because they match a legacy-looking route pattern.
2026-06-08 · All art piece display contexts (gallery grid, exhibit/category cards, work detail) now use a consistent 16:9 container. Images use `object-fit: contain` (letterboxed, no crop). Embed containers use the padding-bottom ratio technique (`height: 0; padding-bottom: 56.25%; overflow: hidden`) with absolutely-positioned `!important` children — this is the deliberate choice over `aspect-ratio` because it cannot be expanded by child content with hardcoded dimensions.
2026-06-08 · Hamburger nav button non-functional on iOS Safari due to `position: sticky` + `-webkit-backdrop-filter: blur()` on `.site-header` dropping synthesised click events (documented WebKit compositing bug). Fixed with `touch-action: manipulation` (CSS) and a `touchend` listener that calls `e.preventDefault()` + `toggle.click()` (JS). Android and desktop unaffected.
