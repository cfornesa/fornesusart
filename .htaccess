RewriteEngine On

# Block direct HTTP access to sensitive files
<FilesMatch "^\.env$|^schema\.sql$|^migrate.*\.sql$|^migrate.*\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

RewriteRule ^(.*)$ public/$1 [L]
